//use itertools::Itertools;
//use std::collections::{*};
//use crate::grid::{*};
//use crate::vector::{*};

pub fn day20(_input: &String) -> (String, String) {
    ("no result".to_string(), "no result".to_string())

}